{
  "name": "github_jobs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "browserSync": "browser-sync start --index public/index.html --browser chrome --server --files=src",
    "browserSyncDist": "browser-sync start --index dist/index.html --browser chrome --server --files=src",
    "build-favicon": "cp -r ./public/favicon.svg ./dist/",
    "build-src": "cp -r ./src ./dist/",
    "build-rollup": "./node_modules/.bin/rollup ./src/components/app/src/GithubJobsApp.js --file dist/src/components/app/src/GithubJobsApp.js --format umd --name GithubJobsApp -m",
    "build-terser": "./node_modules/.bin/terser ./dist/src/components/app/src/GithubJobsApp.js -o ./dist/src/components/app/src/GithubJobsApp.js -m",
    "build-app": "npm run build-favicon && npm run build-src && npm run build-rollup && npm run build-terser"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "navigo": "^8.10.1"
  },
  "devDependencies": {
    "rollup": "^2.39.0",
    "terser": "^5.6.0"
  }
}